{% extends 'base.html.front.twig' %}

{% block title %}Email envoye - AfkArt{% endblock %}

{% block body %}
<style>
  :root {
    --color-cream: #FAF7F2;
    --color-terracotta: #C4704B;
    --color-terracotta-dark: #A85D3B;
    --color-brown: #3D3229;
    --color-brown-light: #5C4A3D;
  }
  * { box-sizing: border-box; }
  body {
    font-family: 'Outfit', system-ui, sans-serif;
    background: var(--color-cream);
    color: var(--color-brown);
  }
  .wrap {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }
  .card {
    background: #fff;
    border-radius: 1.5rem;
    padding: 2.5rem;
    box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    width: 100%;
    max-width: 520px;
    text-align: center;
  }
  h1 {
    font-family: 'Cormorant Garamond', Georgia, serif;
    margin-bottom: .8rem;
  }
  p {
    color: var(--color-brown-light);
    margin-bottom: 1.2rem;
  }
  .btn {
    display: inline-block;
    border: none;
    border-radius: 999px;
    background: var(--color-terracotta);
    color: #fff;
    padding: .75rem 1.5rem;
    text-decoration: none;
  }
  .btn:hover { background: var(--color-terracotta-dark); }
</style>

<div class="wrap">
  <div class="card">
    <h1>Email envoye</h1>
    <p>Un email de reinitialisation a ete envoye. Veuillez verifier votre boite mail puis reinitialiser votre mot de passe.</p>
    <a class="btn" href="{{ path('app_login') }}">Retour a la connexion</a>
  </div>
</div>
{% endblock %}

{% extends 'base.html.front.twig' %}

{% block title %}Nouvel Événement - Afk Art{% endblock %}

{% block body %}
<section style="max-width: 700px; margin: 0 auto; padding: 2rem 1.5rem;">
    <h1 style="font-family: 'Cormorant Garamond', serif; font-size: 2rem; color: #3d2817; margin-bottom: 1.5rem;">Nouvel Événement</h1>
    <p style="color: #5C4A3D; margin-bottom: 1.5rem;">Créez un nouvel atelier, exposition ou événement.</p>
    <a href="{{ path('app_evenement_index') }}" style="display: inline-block; margin-bottom: 1rem; color: #8b5e4a; text-decoration: none; font-weight: 500;">← Retour à la liste</a>
    <div style="background: white; padding: 1.5rem 2rem; border-radius: 12px; box-shadow: 0 2px 12px rgba(0,0,0,0.08);">
        {{ include('evenement/_form.html.twig', {button_label: 'Créer l\'événement'}) }}
    </div>
</section>
<style>
    .form-error-message, .form-group ul { color: #b91c1c; font-size: 0.875rem; margin-top: 0.25rem; list-style: none; padding: 0; }
    .form-group label { display: block; font-weight: 600; color: #374151; margin-bottom: 0.35rem; font-size: 0.9rem; }
</style>
{% endblock %}

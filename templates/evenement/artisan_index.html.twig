{% extends 'base.html.front.twig' %}

{% block title %}Dashboard Artisan - AfkArt{% endblock %}

{% block body %}
<div style="background: #FAF9F6; min-height: 100vh; padding-top: 120px; padding-bottom: 80px;">
    <div class="container">
        
        {# Header / Welcome Section #}
        <div style="display: flex; justify-content: space-between; align-items: flex-end; margin-bottom: 50px;">
            <div>
                <h6 style="color: #C4704B; text-transform: uppercase; letter-spacing: 3px; font-weight: 700; font-size: 0.8rem; margin-bottom: 15px;">Aperçu Global</h6>
                <h1 style="font-family: 'Cormorant Garamond', serif; font-size: 3.5rem; color: #2b2b2b; margin: 0; line-height: 1.1;">
                    Tableau de Bord <br><span style="color: #C4704B;">{{ app.user.prenom }}</span>
                </h1>
            </div>
            
            <a href="{{ path('app_evenement_new') }}" style="background: #C4704B; color: white; padding: 18px 35px; border-radius: 50px; text-decoration: none; font-weight: 700; display: inline-flex; align-items: center; gap: 12px; transition: all 0.3s; box-shadow: 0 10px 25px rgba(196, 112, 75, 0.25);" class="btn-create">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                    <line x1="12" y1="5" x2="12" y2="19"></line>
                    <line x1="5" y1="12" x2="19" y2="12"></line>
                </svg>
                Nouveau Projet
            </a>
        </div>
        {# Intro Section #}
        <div style="background: white; border-radius: 30px; padding: 40px; margin-bottom: 40px; box-shadow: 0 10px 40px rgba(0,0,0,0.02); border: 1px solid #f0f0f0; display: flex; align-items: center; justify-content: space-between;">
            <div style="max-width: 600px;">
                <p style="color: #666; font-size: 1.15rem; line-height: 1.6; margin: 0;">
                    Ici, vous pouvez donner vie à vos projets artistiques, gérer l'apparence de vos expositions et suivre l'évolution de vos créations.
                </p>
            </div>
            <div style="text-align: center; padding-left: 40px; border-left: 2px solid #f0f0f0;">
                <div style="color: #C4704B; font-weight: 700; font-size: 3.5rem; font-family: 'Cormorant Garamond', serif; line-height: 1;">{{ totalEvents }}</div>
                <div style="color: #888; text-transform: uppercase; font-size: 0.75rem; letter-spacing: 2px; font-weight: 600; margin-top: 5px;">Événements Actifs</div>
            </div>
        </div>


        {# Recent Events Section #}
        <div style="margin-bottom: 40px; display: flex; justify-content: space-between; align-items: center;">
            <h2 style="font-family: 'Cormorant Garamond', serif; font-size: 2.2rem; color: #2b2b2b; margin: 0;">Activités Récentes</h2>
        </div>

        {% if evenements is empty %}
            <div style="text-align: center; padding: 80px; background: white; border-radius: 30px; box-shadow: 0 10px 40px rgba(0,0,0,0.02); border: 1px solid #f0f0f0;">
                <div style="font-size: 4rem; margin-bottom: 25px;">✨</div>
                <p style="color: #888; font-size: 1.1rem; margin-bottom: 25px;">Votre aventure commence ici. Créez votre premier événement artistique.</p>
                <a href="{{ path('app_evenement_new') }}" style="color: #C4704B; font-weight: 700; font-size: 1.1rem; text-decoration: none;">C'est parti !</a>
            </div>
        {% else %}
            <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 30px;">
                {% for evenement in evenements %}
                    <div class="mini-card" style="background: white; border-radius: 20px; overflow: hidden; box-shadow: 0 10px 30px rgba(0,0,0,0.03); border: 1px solid rgba(0,0,0,0.04); transition: transform 0.3s;">
                        <div style="height: 180px; position: relative;">
                            {% if evenement.image %}
                                <img src="{{ evenement.image }}" alt="{{ evenement.nom }}" style="width: 100%; height: 100%; object-fit: cover;">
                            {% else %}
                                <div style="width: 100%; height: 100%; background: #f5f5f5; display: flex; align-items: center; justify-content: center; color: #ccc;">AfkArt</div>
                            {% endif %}
                            <div style="position: absolute; top: 12px; right: 12px; background: rgba(255,255,255,0.9); padding: 4px 10px; border-radius: 10px; font-size: 0.7rem; font-weight: 700; color: #C4704B; text-transform: uppercase;">
                                {{ evenement.typeArt }}
                            </div>
                        </div>
                        <div style="padding: 20px;">
                            <h3 style="font-family: 'Cormorant Garamond', serif; font-size: 1.3rem; margin: 0 0 15px 0; color: #2b2b2b;">{{ evenement.nom }}</h3>
                            <div style="display: flex; justify-content: flex-end; align-items: center; pt: 10px; border-top: 1px solid #f9f9f9; padding-top: 15px;">
                                <a href="{{ path('app_evenement_show', {'id': evenement.id}) }}" style="color: #C4704B; text-decoration: none; font-weight: 700; font-size: 0.85rem;">Gérer &rarr;</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}

    </div>
</div>

<style>
    .btn-create:hover {
        transform: translateY(-3px);
        box-shadow: 0 15px 30px rgba(196, 112, 75, 0.3) !important;
        background: #a85a3a !important;
    }
    .mini-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(0,0,0,0.08) !important;
    }
</style>
{% endblock %}

{% extends 'base.back.html.twig' %}

{% block title %}Statistiques Suivi Livraisons - Admin{% endblock %}

{% block back_main %}
        <header class="header">
            <h1>Statistiques des suivis par état</h1>
        </header>
        <div style="background: white; padding: 1.5rem; border-radius: 12px; max-width: 800px;">
            <canvas id="suiviChart" width="400" height="200"></canvas>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
        <script>
            new Chart(document.getElementById('suiviChart'), {
                type: 'bar',
                data: {
                    labels: {{ labels|json_encode|raw }},
                    datasets: [{ label: 'Nombre', data: {{ data|json_encode|raw }}, backgroundColor: '#8b5e4a', borderRadius: 8 }]
                },
                options: { responsive: true, plugins: { legend: { display: false } } }
            });
        </script>
{% endblock %}

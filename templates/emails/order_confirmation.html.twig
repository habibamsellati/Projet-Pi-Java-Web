<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
            border-radius: 10px 10px 0 0;
        }
        .content {
            background: #f9f9f9;
            padding: 30px;
            border: 1px solid #ddd;
        }
        .personalized-message {
            background: #fff;
            border-left: 4px solid #667eea;
            padding: 20px;
            margin: 20px 0;
            font-style: italic;
            color: #555;
        }
        .order-details {
            background: white;
            padding: 20px;
            margin: 20px 0;
            border-radius: 5px;
        }
        .order-details h3 {
            color: #667eea;
            margin-top: 0;
        }
        .article-list {
            list-style: none;
            padding: 0;
        }
        .article-list li {
            padding: 10px;
            border-bottom: 1px solid #eee;
        }
        .article-list li:last-child {
            border-bottom: none;
        }
        .total {
            font-size: 1.3em;
            font-weight: bold;
            color: #667eea;
            text-align: right;
            margin-top: 15px;
            padding-top: 15px;
            border-top: 2px solid #667eea;
        }
        .footer {
            text-align: center;
            padding: 20px;
            color: #777;
            font-size: 0.9em;
            border-radius: 0 0 10px 10px;
            background: #f0f0f0;
        }
        .info-row {
            margin: 10px 0;
        }
        .info-label {
            font-weight: bold;
            color: #667eea;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>✓ Commande Confirmée</h1>
        <p>Commande n°{{ orderNumber }}</p>
    </div>

    <div class="content">
        <div class="personalized-message">
            {{ personalizedMessage|nl2br }}
        </div>

        <div class="order-details">
            <h3>📦 Détails de votre commande</h3>
            
            <div class="info-row">
                <span class="info-label">Client:</span> {{ customerName }}
            </div>
            
            <div class="info-row">
                <span class="info-label">Adresse de livraison:</span> {{ deliveryAddress }}
            </div>
            
            <div class="info-row">
                <span class="info-label">Mode de paiement:</span> {{ paymentMethod }}
            </div>

            <h4 style="margin-top: 20px;">Articles commandés:</h4>
            <ul class="article-list">
                {% for article in articles %}
                <li>
                    <strong>{{ article.titre }}</strong>
                    {% if article.prix %}
                        <span style="float: right; color: #667eea;">{{ article.prix }}€</span>
                    {% endif %}
                </li>
                {% endfor %}
            </ul>

            <div class="total">
                Total: {{ total|number_format(2, ',', ' ') }}€
            </div>
        </div>

        <p style="text-align: center; margin-top: 30px;">
            Vous pouvez suivre l'état de votre commande dans votre espace client.
        </p>
    </div>

    <div class="footer">
        <p>Merci de votre confiance !</p>
        <p style="font-size: 0.85em; color: #999;">
            Cet email a été généré automatiquement, merci de ne pas y répondre.
        </p>
    </div>
</body>
</html>

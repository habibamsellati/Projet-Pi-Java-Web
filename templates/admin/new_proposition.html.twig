{% extends 'admin/base_admin.html.twig' %}

{% block title %}AfkArt | Nouvelle Proposition{% endblock %}
{% block page_title %}Gestion des Propositions{% endblock %}

{% block admin_content %}
    <div class="form-card">
        <div class="form-header">
            <div>
                <h2>Nouvelle Proposition</h2>
                <p>Créez une nouvelle proposition pour un article existant.</p>
            </div>
            <div style="font-size: 40px; opacity: 0.5;">💡</div>
        </div>

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        <div class="form-body">
            <div class="section-title">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
                <h4>Détails de la proposition</h4>
            </div>

            <div class="form-grid">
                <div class="form-group full-width">
                    <label class="form-label-standard">Titre</label>
                    {{ form_widget(form.titre, {'attr': {'class': 'form-control-standard', 'placeholder': 'Titre évocateur'}}) }}
                    <div class="form-error-msg">{{ form_errors(form.titre) }}</div>
                </div>

                <div class="form-group">
                    <label class="form-label-standard">ID Utilisateur</label>
                    {{ form_widget(form.iduser, {'attr': {'class': 'form-control-standard'}}) }}
                    <div class="form-error-msg">{{ form_errors(form.iduser) }}</div>
                </div>

                <div class="form-group">
                    <label class="form-label-standard">Date de création</label>
                    {{ form_widget(form.date, {'attr': {'class': 'form-control-standard'}}) }}
                    <div class="form-error-msg">{{ form_errors(form.date) }}</div>
                </div>

                <div class="form-group full-width">
                    <label class="form-label-standard">Choisir un produit</label>
                    {{ form_widget(form.produit, {'attr': {'class': 'form-control-standard'}}) }}
                    <div class="form-error-msg">{{ form_errors(form.produit) }}</div>
                </div>

                <div class="form-group full-width">
                    <label class="form-label-standard">Description détaillée</label>
                    {{ form_widget(form.description, {'attr': {'class': 'form-control-standard', 'style': 'height: 150px', 'placeholder': 'Expliquez votre proposition en détail...'}}) }}
                    <div class="form-error-msg">{{ form_errors(form.description) }}</div>
                </div>
            </div>
        </div>

        <div class="form-footer">
            <a href="{{ path('admin_propositions') }}" class="btn-form btn-cancel">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
                Retour
            </a>
            <button type="submit" class="btn-form btn-submit">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                Créer la proposition
            </button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}

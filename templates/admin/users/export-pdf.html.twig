<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Liste des Utilisateurs</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Helvetica', Arial, sans-serif;
            color: #333;
            background: white;
        }

        .container {
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 10px;
            color: #3D3229;
            font-size: 24px;
        }

        .info {
            text-align: center;
            color: #666;
            margin-bottom: 20px;
            font-size: 11px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        thead {
            background-color: #3D3229;
            color: white;
        }

        th {
            padding: 12px;
            text-align: left;
            font-weight: bold;
            border: 1px solid #3D3229;
            font-size: 11px;
        }

        td {
            padding: 10px 12px;
            border: 1px solid #ddd;
            font-size: 10px;
        }

        tbody tr:nth-child(even) {
            background-color: #FAF7F2;
        }

        tbody tr:nth-child(odd) {
            background-color: white;
        }

        .role-badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: bold;
            font-size: 9px;
            text-transform: uppercase;
        }

        .role-admin {
            background-color: #fee2e2;
            color: #991b1b;
        }

        .role-livreur {
            background-color: #dbeafe;
            color: #0c4a6e;
        }

        .role-artisant {
            background-color: #fef3c7;
            color: #92400e;
        }

        .role-client {
            background-color: #dcfce7;
            color: #166534;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 4px;
            font-weight: bold;
            font-size: 9px;
        }

        .status-actif {
            background-color: #d1fae5;
            color: #065f46;
        }

        .status-inactif {
            background-color: #f3f4f6;
            color: #6b7280;
        }

        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            text-align: center;
            font-size: 10px;
            color: #666;
        }

        .summary {
            margin-top: 15px;
            padding: 10px;
            background-color: #f5f5f5;
            border-radius: 4px;
            font-size: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📋 Liste des Utilisateurs</h1>
        <div class="info">
            Généré le {{ "now"|date('d/m/Y à H:i:s') }} | AfkArt Platform
        </div>

        {% if users is not empty %}
            <table>
                <thead>
                    <tr>
                        <th>Prénom</th>
                        <th>Nom</th>
                        <th>Email</th>
                        <th>Rôle</th>
                        <th>Statut</th>
                        <th>Date de création</th>
                    </tr>
                </thead>
                <tbody>
                    {% for user in users %}
                        <tr>
                            <td>{{ user.prenom }}</td>
                            <td>{{ user.nom }}</td>
                            <td>{{ user.email }}</td>
                            <td>
                                <span class="role-badge role-{{ user.role|lower }}">
                                    {% if user.role == 'CLIENT' %}
                                        Client
                                    {% elseif user.role == 'ARTISANT' %}
                                        Artisan
                                    {% elseif user.role == 'ADMIN' %}
                                        Admin
                                    {% elseif user.role == 'LIVREUR' %}
                                        Livreur
                                    {% else %}
                                        {{ user.role }}
                                    {% endif %}
                                </span>
                            </td>
                            <td>
                                <span class="status-badge status-{{ user.statut }}">
                                    {{ user.statut|capitalize }}
                                </span>
                            </td>
                            <td>{{ user.datecreation|date('d/m/Y H:i') }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <div class="summary">
                <strong>Total d'utilisateurs :</strong> {{ users|length }}<br>
                <strong>Admins :</strong> {{ users|filter(u => u.role == 'ADMIN')|length }}<br>
                <strong>Artisans :</strong> {{ users|filter(u => u.role == 'ARTISANT')|length }}<br>
                <strong>Clients :</strong> {{ users|filter(u => u.role == 'CLIENT')|length }}<br>
                <strong>Livreurs :</strong> {{ users|filter(u => u.role == 'LIVREUR')|length }}
            </div>

            <div class="footer">
                <p>© 2026 AfkArt - Tous droits réservés</p>
            </div>
        {% else %}
            <p>Aucun utilisateur trouvé.</p>
        {% endif %}
    </div>
</body>
</html>

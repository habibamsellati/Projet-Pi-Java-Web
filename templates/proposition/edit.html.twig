{% extends 'base.html.twig' %}

{% block title %}Modifier Proposition{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-xl-7">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h2 class="h3 fw-bold mb-0">Modifier la proposition</h2>
                    <a href="{{ path('home') }}" class="btn btn-outline-secondary fw-bold px-3 py-1 text-uppercase rounded-pill" style="font-size: 0.8rem;">
                        <i class="fas fa-home me-2"></i>Retour au site
                    </a>
                </div>
                {{ include('proposition/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="mt-4 text-end">
                     {{ include('proposition/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
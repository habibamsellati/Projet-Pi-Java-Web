<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Validation Officielle - AfkArt Security</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&family=Cormorant+Garamond:wght@500;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            background: #050505; 
            min-height: 100vh; 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            padding: 20px; 
            font-family: 'Outfit', sans-serif; 
            color: white;
            overflow-x: hidden;
        }
        .container {
            background: linear-gradient(145deg, rgba(255,255,255,0.05) 0%, rgba(255,255,255,0.01) 100%);
            backdrop-filter: blur(30px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 40px;
            width: 100%;
            max-width: 450px;
            padding: 60px 30px;
            text-align: center;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
            position: relative;
        }
        .branding {
            margin-bottom: 40px;
            opacity: 0.8;
            letter-spacing: 5px;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
        }
        .brand-accent { color: #C4704B; }
        
        .badge-wrapper {
            position: relative;
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
        }
        .ripple {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            opacity: 0.2;
            animation: ripple-effect 2s infinite;
        }
        .badge {
            position: relative;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .badge.success { background: #10b981; box-shadow: 0 0 40px rgba(16, 185, 129, 0.5); }
        .badge.error { background: #ef4444; box-shadow: 0 0 40px rgba(239, 68, 68, 0.5); }
        .ripple.success { background: #10b981; }

        h1 { font-family: 'Cormorant Garamond', serif; font-size: 2.5rem; margin-bottom: 5px; font-weight: 500; }
        .success-text { color: #10b981; }
        .error-text { color: #ef4444; }

        .status-badge {
            font-weight: 700;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }
        .dot { width: 8px; height: 8px; border-radius: 50%; }

        .data-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 30px;
            padding: 35px;
            text-align: left;
            margin-bottom: 40px;
        }
        .label { color: rgba(255,255,255,0.4); font-size: 0.65rem; text-transform: uppercase; letter-spacing: 2px; margin-bottom: 8px; }
